# ================================================================
# Test Profile — H2 In-Memory Database Configuration
# ================================================================

# DataSource Configuration
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA / Hibernate test settings
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# H2 Console Configuration
spring.h2.console.enabled=true

# Security Settings (Aligned with JwtUtils.java)
zest.app.jwtSecret=test-secret-key-that-is-long-enough-for-hmac-sha256-algorithm-testing
zest.app.jwtExpirationMs=900000
zest.app.jwtRefreshExpirationMs=604800000

# Logging Configuration
logging.level.root=WARN
logging.level.com.zest.product=DEBUG
